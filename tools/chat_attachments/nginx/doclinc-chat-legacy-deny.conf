# Install only after the legacy attachment migration reports zero remaining rows.
# Include this snippet inside every public Doclinc server block.
location ^~ /uploads/chat_images/ {
    access_log off;
    log_not_found off;
    return 404;
}
